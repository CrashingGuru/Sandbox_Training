# NCEMA Crisis Communication Guidelines

- **Doc ID:** 5.2
- **Issuing body:** NCEMA
- **Referenced in notebook row(s):** 5
- **Target filename (once downloaded):** `5.2_NCEMA_Crisis_Communication_Guidelines.html`

## Abstract

Guidance on escalation channels and communication protocols during crisis events (including telecom outages). Prescribes how early-warning alerts should be routed to NOC, emergency services and public.

## Access links

- <https://www.ncema.gov.ae/en/media-center/publications/publications.aspx>

## How to download

The Cowork sandbox is firewalled from direct outbound HTTP to UAE gov domains, so this file could not be fetched automatically. Two options to populate this folder:

1. **Run the top-level `download_all.sh` script** (recommended — pulls everything at once):
   ```bash
   cd "$(dirname "$0")/.."   # up to PolicyData/
   bash download_all.sh
   ```

2. **Fetch just this doc** with curl:
   ```bash
   curl -L -A "Mozilla/5.0" -o "5.2_NCEMA_Crisis_Communication_Guidelines.html" "https://www.ncema.gov.ae/en/media-center/publications/publications.aspx"
   ```

If the primary link fails, try the alternate URLs above.
