# UAE Information Assurance (IA) Standards — NESA / SIA (now under CSC)

- **Doc ID:** 4.2
- **Issuing body:** Cyber Security Council (formerly NESA / Signals Intelligence Agency)
- **Referenced in notebook row(s):** 9
- **Target filename (once downloaded):** `4.2_UAE_Information_Assurance_Standards_NESA_SIA.html`

## Abstract

Family of information-assurance controls mandating cryptographic, physical, personnel and operational controls for federal entities. Referenced for privacy-preserving ML (federated learning) and secure data pipelines.

## Access links

- <https://csc.gov.ae/>

## How to download

The Cowork sandbox is firewalled from direct outbound HTTP to UAE gov domains, so this file could not be fetched automatically. Two options to populate this folder:

1. **Run the top-level `download_all.sh` script** (recommended — pulls everything at once):
   ```bash
   cd "$(dirname "$0")/.."   # up to PolicyData/
   bash download_all.sh
   ```

2. **Fetch just this doc** with curl:
   ```bash
   curl -L -A "Mozilla/5.0" -o "4.2_UAE_Information_Assurance_Standards_NESA_SIA.html" "https://csc.gov.ae/"
   ```

If the primary link fails, try the alternate URLs above.
