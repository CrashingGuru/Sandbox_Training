# Mohammed Bin Rashid Space Centre (MBRSC) — Programmes & Data

- **Doc ID:** 8.3
- **Issuing body:** MBRSC (Dubai)
- **Referenced in notebook row(s):** 7
- **Target filename (once downloaded):** `8.3_MBRSC_Programmes_and_Data.html`

## Abstract

Dubai's space centre — home of KhalifaSat, MBZ-SAT, Hope Probe, and the UAE Astronaut Programme. Publishes EO datasets and knowledge-hub content useful for cropland / land-use ML in UAE-specific tiles.

## Access links

- <https://www.mbrsc.ae/>
- <https://www.mbrsc.ae/knowledge-hub/>

## How to download

The Cowork sandbox is firewalled from direct outbound HTTP to UAE gov domains, so this file could not be fetched automatically. Two options to populate this folder:

1. **Run the top-level `download_all.sh` script** (recommended — pulls everything at once):
   ```bash
   cd "$(dirname "$0")/.."   # up to PolicyData/
   bash download_all.sh
   ```

2. **Fetch just this doc** with curl:
   ```bash
   curl -L -A "Mozilla/5.0" -o "8.3_MBRSC_Programmes_and_Data.html" "https://www.mbrsc.ae/"
   ```

If the primary link fails, try the alternate URLs above.
