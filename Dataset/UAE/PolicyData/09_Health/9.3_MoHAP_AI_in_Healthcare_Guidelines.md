# MoHAP AI in Healthcare Guidelines

- **Doc ID:** 9.3
- **Issuing body:** Ministry of Health and Prevention (MoHAP)
- **Referenced in notebook row(s):** 8
- **Target filename (once downloaded):** `9.3_MoHAP_AI_in_Healthcare_Guidelines.html`

## Abstract

Federal MoHAP guidance on deployment of AI in healthcare — clinical-validation SOPs, data-quality requirements, human-in-the-loop obligations, adverse-event reporting.

## Access links

- <https://mohap.gov.ae/en/services/electronic-health-services>
- <https://mohap.gov.ae/>

## How to download

The Cowork sandbox is firewalled from direct outbound HTTP to UAE gov domains, so this file could not be fetched automatically. Two options to populate this folder:

1. **Run the top-level `download_all.sh` script** (recommended — pulls everything at once):
   ```bash
   cd "$(dirname "$0")/.."   # up to PolicyData/
   bash download_all.sh
   ```

2. **Fetch just this doc** with curl:
   ```bash
   curl -L -A "Mozilla/5.0" -o "9.3_MoHAP_AI_in_Healthcare_Guidelines.html" "https://mohap.gov.ae/en/services/electronic-health-services"
   ```

If the primary link fails, try the alternate URLs above.
